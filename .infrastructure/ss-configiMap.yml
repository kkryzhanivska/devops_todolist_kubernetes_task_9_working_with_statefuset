apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-config
  namespace: mysql
data:
  init.sql: | 
    GRANT ALL PRIVILEGES ON todoapp_db.* TO 'todoapp_user'@'%';
    USE todoapp_db;
    CREATE TABLE counter (
        id INT AUTO_INCREMENT PRIMARY KEY,
        value INT
    );